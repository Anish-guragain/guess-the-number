'use strict';
/*
const temp = [3, -2, -6, -1, 'error', 9, 13, 17, 15, 19, 20];

const calctemp = function (temperature) {
  let max = temperature[0];
  let min = temperature[0];
  for (let i = 1; i < temperature.length; i++) {
    const curtemp = temperature[i];
    if (curtemp > max) max = curtemp;
    if (curtemp < min) min = curtemp;
  }

  console.log(max, min);
  return max - min;
};

const tempaa = calctemp(temp);
console.log(tempaa);



const kelvin = function () {
  const measurement = {
    type: 'temp',
    unit: 'celsius',
    value: Number(prompt(`degree celsius`)),
  };

  const kelvinchange = measurement.value + 273;
  return kelvinchange;
};
console.log(kelvin());

////////////////coding challenge///////////////////

const temp = [17, 21, 23];
const temp2 = [12, 5, -5, 0, 4];

// console.log(
//   `...${temp[0]}c in first day ...${temp[1]}c in second day ...${temp[2]}c in a third day`
// );

const printforecst = function (arr) {
  let str = '';
  for (let i = 0; i < arr.length; i++) {
    str = str + `${arr[i]}c in ${i + 1}days...`;
  }

  console.log(`...` + str);
};

printforecst(temp);
printforecst(temp2);

*/
